{
    "name": "Environment Variables",
    "id": "env",
    "version": "1.0.0",
    "description": "Configures environment variables from GitHub repository secrets",
    "options": {
        "githubRepo": {
            "type": "string",
            "description": "GitHub repository to fetch environment variables from",
            "default": ""
        },
        "envFile": {
            "type": "string",
            "description": "Path to environment file to create",
            "default": "/usr/local/etc/devcontainer.env"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/github-cli"
    ]
}
